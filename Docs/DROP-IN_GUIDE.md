# Drop‑In Guide — How to Use These Docs

1) Create a folder `/docs/` in your repo (if not present).
2) Upload all these `.md` files into `/docs/`.
3) Point your root `README.md` to this directory.
4) Give the coding agent the task list in `CONTRIBUTING_FOR_AGENT.md` to begin implementing the slice.
